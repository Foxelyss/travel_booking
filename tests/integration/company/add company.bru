meta {
  name: add company
  type: http
  seq: 1
}

post {
  url: {{host}}/api/company/
  body: json
  auth: inherit
}

body:json {
  {{company_creation}}
}

script:post-response {
  bru.setVar("latest_company", res.body.Id);
}

settings {
  encodeUrl: true
  timeout: 0
}
