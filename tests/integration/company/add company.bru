meta {
  name: add company
  type: http
  seq: 1
}

post {
  url: {{host}}/api/company/
  body: json
  auth: inherit
}

body:json {
  {{company_creation}}
}

settings {
  encodeUrl: true
  timeout: 0
}
