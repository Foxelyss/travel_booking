meta {
  name: get company copy
  type: http
  seq: 4
}

get {
  url: {{host}}/api/company/:id
  body: none
  auth: inherit
}

params:path {
  id: {{latest_company}}
}

tests {
  test("Content of Point should be the same", ()=>{
    let a = res.getBody();
    delete a.id;
    expect(a).to.be.eql(bru.getVar("mean_change_result"));
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
