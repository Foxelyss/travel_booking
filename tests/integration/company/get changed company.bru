meta {
  name: get changed company
  type: http
  seq: 4
}

get {
  url: {{host}}/api/company/:id
  body: none
  auth: inherit
}

params:path {
  id: {{latest_company}}
}

tests {
  test("Content of Company should be the same", ()=>{
    expect(res.body).to.include(bru.getVar("company_change_result"));
  });
  
  test("Content type is JSON", () => {
    expect(res.getHeader("content-type")).to.contain("application/json");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
