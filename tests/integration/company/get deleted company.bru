meta {
  name: get deleted company
  type: http
  seq: 6
}

get {
  url: {{host}}/api/company/:id
  body: none
  auth: inherit
}

params:path {
  id: {{latest_company}}
}

tests {
  test("Shall return code 404", () => {
    expect(res.status).to.be.eql(404);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
