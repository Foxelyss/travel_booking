meta {
  name: change company
  type: http
  seq: 3
}

patch {
  url: {{host}}/api/point/:id
  body: json
  auth: inherit
}

params:path {
  id: 
}

body:json {
  {{company_change}}
}

settings {
  encodeUrl: true
  timeout: 0
}
