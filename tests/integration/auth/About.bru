meta {
  name: About
  type: http
  seq: 4
}

get {
  url: {{host}}/api/auth/about
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:multipart-form {
  email: asd
  password: 123
}

tests {
  test("Return 200", () => {
    expect(res.status).to.equal(200);
  });
  
  test("Content of Account should be the same", ()=>{
    expect(res.body).to.include(bru.getVar("account_check"));
  });
  
  test("Content type is JSON", () => {
    expect(res.getHeader("content-type")).to.contain("application/json");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
