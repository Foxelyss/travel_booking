meta {
  name: change point
  type: http
  seq: 3
}

patch {
  url: {{host}}/api/transport/:id
  body: json
  auth: inherit
}

params:path {
  id: {{latest_transport}}
}

body:json {
  {{transport_change}}
}

settings {
  encodeUrl: true
  timeout: 0
}
