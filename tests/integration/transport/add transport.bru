meta {
  name: add transport
  type: http
  seq: 1
}

post {
  url: {{host}}/api/transport/
  body: json
  auth: inherit
}

body:json {
  {{transport_creation}}
}

body:multipart-form {
  asfd: 
}

script:post-response {
  bru.setVar("latest_transport", res.body.id);
}

tests {
  test("Return 200", () => {
    expect(res.status).to.equal(200);
  });
  
  test("Content of Point should be the same", ()=>{
    expect(res.body).to.include(bru.getVar("transport_creation"));
  });
  
  test("Content type is JSON", () => {
    expect(res.getHeader("content-type")).to.contain("application/json");
  });
  
}

settings {
  encodeUrl: true
  timeout: 0
}
