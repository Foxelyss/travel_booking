meta {
  name: get changed transport
  type: http
  seq: 4
}

get {
  url: {{host}}/api/transport/:id
  body: none
  auth: inherit
}

params:path {
  id: {{latest_transport}}
}

body:multipart-form {
  asfd: 
}

tests {
  test("Return 200", () => {
    expect(res.status).to.equal(200);
  });
  
  test("Content of Point should be the same", ()=>{
    expect(res.body).to.include(bru.getVar("transport_change_result"));
  });
  
  test("Content type is JSON", () => {
    expect(res.getHeader("content-type")).to.contain("application/json");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
