meta {
  name: delete point copy
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/point/login
  body: none
  auth: inherit
}

tests {
  test("Shall return code 200", () => {
    expect(res.status).to.be.eql(200);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
