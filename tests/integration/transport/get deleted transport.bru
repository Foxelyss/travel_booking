meta {
  name: get deleted transport
  type: http
  seq: 6
}

get {
  url: {{host}}/api/transport/:id
  body: none
  auth: inherit
}

params:path {
  id: {{latest_transport}}
}

body:multipart-form {
  asfd: 
}

tests {
  test("Shall return code 404", () => {
    expect(res.status).to.be.eql(404);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
