meta {
  name: get point copy
  type: http
  seq: 4
}

get {
  url: {{host}}/api/point/:id
  body: json
  auth: inherit
}

params:path {
  id: {{latest_point}}
}

tests {
  test("Content of Point should be the same", ()=>{
    expect(res.body).to.include(bru.getVar("point_change_result");
  })
}

settings {
  encodeUrl: true
  timeout: 0
}
