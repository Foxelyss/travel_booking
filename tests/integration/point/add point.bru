meta {
  name: add point
  type: http
  seq: 1
}

post {
  url: {{host}}/api/point
  body: json
  auth: inherit
}

body:json {
  {{point_creation}}
}

script:post-response {
  bru.setVar("latest_point", res.body.id);
}

tests {
  test("Return 200", () => {
    expect(res.status).to.equal(200);
  });
  
}

settings {
  encodeUrl: true
  timeout: 0
}
