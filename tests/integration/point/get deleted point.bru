meta {
  name: get deleted point
  type: http
  seq: 6
}

get {
  url: {{host}}/api/point/:id
  body: json
  auth: inherit
}

params:path {
  id: {{latest_point}}
}

tests {
  test("Shall return code 404", () => {
    expect(res.status).to.be.eql(404);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
