vars:pre-request {
  host: http://localhost:5165
}

tests {
  test("Response time is less than 250ms", () => {
    expect(res.getResponseTime()).to.be.below(250);
  });
  
  test("Content type is JSON", () => {
    expect(res.getHeader("content-type")).to.contain("application/json");
  });
  
}
