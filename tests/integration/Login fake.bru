meta {
  name: Login fake
  type: http
  seq: 2
}

post {
  url: {{host}}/api/auth/login
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  email: asd@asd.com
  password: 123
}

tests {
  test("Shall return code 4XX", () => {
    expect(res.status).to.be.oneOf([400, 401, 403]);
  });
  
  test("Content type is JSON", () => {
    expect(res.getHeader("content-type")).to.contain("application/json");
  });
  
}

settings {
  encodeUrl: true
  timeout: 0
}
