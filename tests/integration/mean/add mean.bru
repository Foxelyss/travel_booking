meta {
  name: add mean
  type: http
  seq: 1
}

post {
  url: {{host}}/api/mean/
  body: json
  auth: inherit
}

body:json {
  {{mean_creation}}
}

script:post-response {
  bru.setVar("latest_mean", res.body.Id);
}

settings {
  encodeUrl: true
  timeout: 0
}
