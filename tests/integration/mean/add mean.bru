meta {
  name: add mean
  type: http
  seq: 1
}

post {
  url: {{host}}/api/mean/
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Автобус"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
