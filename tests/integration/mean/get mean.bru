meta {
  name: get mean
  type: http
  seq: 2
}

get {
  url: {{host}}/api/mean/:id
  body: json
  auth: inherit
}

params:path {
  id: {{latest_mean}}
}

body:json {
  {
    "name": "asdfas"
  }
}

tests {
  test("Content of Point should be the same", ()=>{
    let a = res.getBody();
    delete a.id;
    expect(a).to.be.eql(bru.getVar("mean_creation"));
  })
}

settings {
  encodeUrl: true
  timeout: 0
}
