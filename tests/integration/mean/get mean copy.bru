meta {
  name: get mean copy
  type: http
  seq: 4
}

get {
  url: {{host}}/api/mean/:id
  body: json
  auth: inherit
}

params:path {
  id: {{latest_mean}}
}

body:json {
  {
    "name": "asdfas"
  }
}

tests {
  test("Content of Point should be the same", ()=>{
    expect(res.body).to.include(bru.getVar("mean_change_result"));
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
