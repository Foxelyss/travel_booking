meta {
  name: get mean copy copy
  type: http
  seq: 6
}

get {
  url: {{host}}/api/mean/:id
  body: json
  auth: inherit
}

params:path {
  id: {{latest_mean}}
}

body:json {
  {
    "name": "asdfas"
  }
}

tests {
  test("Shall return code 404", () => {
    expect(res.status).to.be.eql(404);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
