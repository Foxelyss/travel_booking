meta {
  name: change mean
  type: http
  seq: 3
}

patch {
  url: {{host}}/api/point/login
  body: json
  auth: inherit
}

body:json {
  {{mean_change}}
}

settings {
  encodeUrl: true
  timeout: 0
}
