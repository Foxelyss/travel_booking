meta {
  name: change mean
  type: http
  seq: 3
}

patch {
  url: {{host}}/api/mean/:id
  body: json
  auth: inherit
}

params:path {
  id: {{latest_mean}}
}

body:json {
  {{mean_change}}
}

settings {
  encodeUrl: true
  timeout: 0
}
