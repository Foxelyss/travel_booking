meta {
  name: delete mean
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/mean/:id
  body: none
  auth: inherit
}

params:path {
  id: {{latest_mean}}
}

settings {
  encodeUrl: true
  timeout: 0
}
