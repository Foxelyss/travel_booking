meta {
  name: return
  type: http
  seq: 2
}

post {
  url: {{host}}/api/booking/return
  body: formUrlEncoded
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:form-urlencoded {
  id: 2
}

settings {
  encodeUrl: true
  timeout: 0
}
